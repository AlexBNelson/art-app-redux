import { connect } from 'react-redux'
import ArticleThumbnailComponent from './ArticleThumbnailComponent';

const mapStateToProps = state => ({
})

export default connect(
    mapStateToProps
)(ArticleThumbnailComponent)